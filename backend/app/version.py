"""Canonical backend product release identity.

Keep this value aligned with the repository root VERSION file. CI enforces the
contract so containerized backend builds do not need files outside the backend
build context.
"""

APP_VERSION = "2.1.0"
