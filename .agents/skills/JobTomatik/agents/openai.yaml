interface:
  display_name: "JobTomatik"
  short_description: "Repo-specific patterns and workflows for JobTomatik"
  default_prompt: "Use the JobTomatik repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true